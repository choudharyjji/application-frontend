import React, { ReactElement } from 'react';

const AcceptedPage = (): ReactElement => (
  <>
    <h2 className="text-3xl font-extrabold mb-5 xl:text-4xl">
      Your lead has been accepted
    </h2>
  </>
);

export default AcceptedPage;
